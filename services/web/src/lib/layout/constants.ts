export const DEFAULT_PAGE_WIDTH = 794; // px (A4 portrait content area)
export const DEFAULT_PAGE_HEIGHT = 1123; // px (A4 portrait content area)
export const DEFAULT_PAGE_MARGIN_TOP = 40;
export const DEFAULT_PAGE_MARGIN_BOTTOM = 40;
export const DEFAULT_PAGE_MARGIN_LEFT = 40;
export const DEFAULT_PAGE_MARGIN_RIGHT = 40;

// Typography defaults
export const DEFAULT_FONT_SIZE = 16; // px
export const DEFAULT_LINE_HEIGHT = 20; // px
export const DEFAULT_FONT_FAMILY =
  'system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';

export const MAX_PAGES = 200; // safety limit to prevent infinite looping
